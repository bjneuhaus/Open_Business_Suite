# tests/

Automatisierte Tests für die Sovereign Business Suite.

Dieses Verzeichnis enthält die pytest-basierten Tests für den unter
`src/` liegenden Anwendungscode.

Zum Zeitpunkt von R001 (Repository-Struktur) ist dieses Verzeichnis noch
leer. Testkonfiguration (`pytest`, Fixtures, Testabhängigkeiten) wird ab
R002 (Python-Projekt) bzw. R003 (Code Quality) eingerichtet.
